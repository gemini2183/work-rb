---
name: client-decisions-log
description: Append-only стратегический лог решений по клиенту SED Латвия
updated: 2026-09-01
---

# Решения — SED Латвия

Формат записи: `## [ГГГГ-ММ-ДД] Кампания/тема | действие | гипотеза | результат`

Сюда — только стратегически значимое (смена подхода, реакция на результаты по крупным
срезам), не рутинные правки — им место в логах конкретных кампаний.

---

## [2026-09-01] Ringostat подмена номера, хедер | обнаружен конфликт скриптов | — | найден проблемный код на сайте

При разборе HTML-разметки хедера главной страницы (LV `/` и RU `/ru/`) для настройки
подмены номера в Ringostat обнаружено:

1. Сайт на движке Tilda (`data-tilda-project-id="21865306"`).
2. Основной номер клиента: `+371-67-660-725` (`tel:+37167660725`).
3. В сыром HTML (до выполнения JS) пункт меню хедера содержит битые `tel:`-ссылки:
   - LV-версия: `tel:+337167660725` (опечатка в коде страны — 337 вместо 371).
   - RU-версия: `tel:+37120007879` (другой номер целиком, не совпадает с видимым текстом).
4. На странице уже стоит кастомный inline-скрипт (переменные `CORRECT_HREF`/
   `CORRECT_TEXT`, функция `fixPhoneElement`), который при `DOMContentLoaded`/`load`
   находит все `a[href^="tel:"]` и принудительно переписывает их на корректный номер,
   а также держит `MutationObserver` на `document.documentElement` (`subtree: true`,
   следит за `href` и текстом), который откатывает **любое** последующее изменение
   этих ссылок обратно на статичный номер.
5. Из-за этого MutationObserver скрипт-фиксер будет откатывать подмену номера
   Ringostat почти сразу после того, как коллтрекинг её произведёт — подмена не
   удержится в DOM.

Решение (согласовано с пользователем): подмену Ringostat настраивать только после
того, как:
- битые `tel:`-ссылки будут исправлены вручную в самих виджетах Tilda (не через
  скрипт), и
- скрипт-фиксер будет полностью удалён (после ручной правки он не нужен, а его
  MutationObserver гарантированно ломает подмену Ringostat).

См. задачи в [[Задачи]].

## [2026-09-01] Ringostat подмена номера | ревизия кабинета | прошлогодняя настройка "на весь сайт" почти вся отвалилась | найдена и частично исправлена причина, хедер всё ещё не работает

Полная ревизия кабинета Ringostat (скриншоты пользователя) показала масштаб иначе,
чем предполагалось изначально:

**Каналы и пулы (в порядке, не связаны с проблемой):**
- Канал "All transitions": `utm_campaign` не содержит "pmax" — весь трафик кроме
  Performance Max. Пул (динамическая подмена): `37167660729`, `37167660728`,
  `37167660749`.
- Канал "pmax": `utm_source=google&utm_medium=cpc&utm_campaign содержит "pmax"`.
  Пул (статическая): `37167660726`.

**XPath-правила (36 штук изначально) — корень проблемы:**
Подмена была настроена в прошлом году "на весь сайt", но почти все правила были
заведены не по классу, а по хрупким числовым Tilda-id конкретных блоков
(`nav1893659011`, `rec1967266793` и т.п.) — отдельное правило на каждую
LV/RU-страницу и на каждую sticky/обычную копию хедера. Ожидаемо часть из них
омертвела при пересборке вёрстки в Tilda. Единственные живые class-based правила
на момент ревизии: `header` (класс `t1261__container`), `footer lv`/`footer ru`
(класс `t420__descr`+текст "KONTAKTI"/"КОНТАКТЫ"), `contact lv` (текст
"Adrese"/"Адрес"), `ringo-phone` (спец-класс `ringo-phone`, используется в
юридических страницах политики конфиденциальности LV и RU вместе с full-text
масками на весь текст политики).

**Найдено и исправлено:**
1. Правило `header` имело `geoGroup: "not set"` вместо `"All"`, как у всех
   остальных правил — из-за этого сервер Ringostat (`changed_number`) вообще не
   выдавал номер для этого правила (индекс правила был в списке безномерных).
   Исправлено пользователем на "All" — сервер начал выдавать номер (подтверждено
   прямым запросом к `analytics.ringostat.net/changed_number/`).
2. На LV-версии обнаружено тройное перекрытие одного физического элемента
   хедера: новое `header` + два старых хрупких id-правила (`header lv`,
   `header floating lv`), которые ещё оставались живыми (Tilda-id не изменился).
   Удалены как дубли.
3. `customUserSettings.observeDOM` был `false` (в отличие от рабочего проекта
   [[../EkspertAgro/index|EkspertAgro]], где `true`) — включён по аналогии.

**Ни одно из трёх исправлений не решило проблему** — номер в хедере (LV и RU,
обе копии — обычная и sticky) остаётся статичным `+371-67-660-725` и после всех
правок, проверено и в headless Playwright, и в реальном браузере пользователя
(идентичный результат — не проблема тестовой среды).

**Технически подтверждено через `document.evaluate`-перехват на странице:**
сам XPath правила `header` находит ровно 2 элемента (обычная + sticky копия),
БЕЗ ошибки выполнения. То есть проблема не в XPath и не в поиске элементов —
подмена не доходит до финального шага записи в DOM по неизвестной причине
глубже уровня клиентского кода, доступного для чтения снаружи (минифицированный
скрипт Ringostat, внутренняя логика применения маски — не удалось точно
локализовать без доступа к внутренним логам сервиса).

Также на странице присутствует посторонняя JS-ошибка `Invalid regular
expression: Unterminated group` — проверено, что не связана с нашими
XPath-правилами (осталась после удаления full-text правил политики
конфиденциальности), причина не установлена, вероятно не относится к проблеме.

Скрипт Ringostat установлен через GTM (`GTM-TMV7VSWQ`, тег "Ringostat script",
Пользовательский HTML, триггер All Pages) — код идентичен эталонному коду
установки из кабинета, расхождений не найдено.

**Статус:** подготовлено и передано пользователю обращение в поддержку
Ringostat с полным repro (см. текст в решении — не хранится в вики, отправлен
напрямую). Ждём ответа поддержки — без доступа к их внутренним логам обработки
дальнейшая диагностика с нашей стороны неэффективна. Футер RU по-прежнему
работает (правило `footer ru`, номер ротируется корректно), это единственное
подтверждённо рабочее место подмены на сайте на данный момент.

Известно, что важно проверить/сделать после получения ответа поддержки:
- Хедер LV и RU (обе копии) — основная незакрытая задача.
- Правило `footer ru` рвёт `href` (кликабельность) при подмене — эффект не
  переисследован после всех этих правок, нужно перепроверить отдельно.
- `footer lv` и `contact lv` тоже были в списке безномерных изначально (как и
  `header` до правки) — не проверено, чинит ли их аналогичная правка ГЕО-группы
  (если у них тоже `not set`).
- Остаётся открытым вопрос про битые `tel:`-ссылки в исходной вёрстке Tilda и
  самопальный скрипт-фиксер (`CORRECT_HREF`/`fixPhoneElement`) из предыдущей
  записи — не тронуты в ходе этой ревизии, актуальность не проверена заново.

См. [[Задачи]].

## [2026-09-01] Ringostat подмена номера, хедер | найдена и устранена настоящая причина | скрипт-фиксер из первой записи откатывал подмену через MutationObserver | хедер заработал на LV и RU

Разгадка всей проблемы из предыдущей записи оказалась именно та, что предполагалась
в самой первой записи этого лога (2026-09-01, "обнаружен конфликт скриптов"), просто
подтверждение затянулось из-за параллельного расследования конфигурации кабинета
Ringostat, которое увело в сторону.

Пользователь нашёл сам скрипт-фиксер вручную в Tilda — он оказался в глобальной
head/body-вставке сайта (не в отдельном HTML-блоке зоны Header, как предполагалось
изначально). Код скрипта — тот же, что был вычислен по сырому HTML в первой записи
(`CORRECT_HREF`/`CORRECT_TEXT`/`fixPhoneElement`/`MutationObserver`).

Механизм подтверждён: `MutationObserver` следит за `document.documentElement`
(`subtree: true`, `attributeFilter: ['href']`) и откатывает ЛЮБОЕ изменение
`href` у `a[tel:]` обратно на статичный корректный номер `+371-67-660-725` —
синхронно, в том же тике event loop, что и изменение. Именно поэтому вся
диагностика предыдущей записи (перехват `document.evaluate`, polling `href`
каждые 20мс, серверные логи Ringostat) не могла поймать момент подмены — Ringostat
реально подставлял номер в DOM, но откат происходил быстрее, чем что-либо успевало
это прочитать.

**Действия:**
1. Исправлена битая `tel:`-ссылка в пункте меню хедера LV (`tel:+337167660725` →
   `tel:+37167660725`) прямо в настройках пункта меню Tilda.
2. Скрипт-фиксер полностью удалён из head/body-вставки сайта.
3. Изменения опубликованы.

**Результат подтверждён** (Playwright, свежая загрузка после публикации):
- Скрипт-фиксер отсутствует в отрендеренном DOM обеих версий (проверено
  по отсутствию строки `CORRECT_HREF`).
- Хедер LV: `<a href="tel:+37167660728">+371-67-660-728</a>` — подмена работает,
  номер из пула "All transitions", маска отработала корректно (полный формат
  с дефисами).
- Хедер RU: `<a href="tel:+37167660749">` — аналогично, подмена работает.

Хедер (обычная и sticky копии, LV и RU) — закрыт, первая задача проекта выполнена.

**Не проверено повторно после этого фикса** (осталось от предыдущей записи):
- Актуальность обращения в поддержку Ringostat — можно отозвать/закрыть, раз
  причина найдена и не относится к их стороне.
- Правило `footer ru` на предмет разрыва кликабельности `href`.
- Правило RU-меню хедера — была ли там аналогичная опечатка (`tel:+37120007879`
  из самого первого прогона curl) отдельно от LV, не проверялась и не правилась
  в этой сессии явно (возможно, тоже была одной и той же причиной — скрипт-фиксер
  маскировал её на лету, как и LV).
- Полный аудит битых номеров по всему сайту (не только хедер) — задача
  изначально про многостраничный сайт, пока разобрана только главная.

См. [[Задачи]].

## [2026-09-01] Ringostat подмена номера, хедер | найдена вторая часть скрипта-фиксера (CSS) | номер пропал визуально после удаления JS-половины | устранено, хедер полностью работает

Сразу после устранения проблемы из предыдущей записи номер в хедере обеих версий
пропал ВИЗУАЛЬНО (пустое место в меню после "Kontakti"), хотя элемент оставался в
DOM с правильным подменённым номером внутри.

Причина — скрипт-фиксер оказался связкой из ДВУХ частей, а удалена была только
JS-половина:
1. JS (уже удалён в предыдущей записи) — чинил `href`/текст `tel:`-ссылок и
   помечал починенные классом `phone-fixed`.
2. **CSS** (найден в `custom.css` сайта, раздел "Редактирование пользовательского
   CSS-кода" в настройках Tilda) — прятал АБСОЛЮТНО ВСЕ `tel:`-ссылки на сайте по
   умолчанию, и показывал обратно только те, что помечены классом `phone-fixed`:

       a[href^="tel:"] { visibility: hidden; }
       a.phone-fixed { visibility: visible !important; }

После удаления JS-половины класс `phone-fixed` перестал кем-либо проставляться
(ни удалённым скриптом, ни Ringostat — тот про этот класс не знает), поэтому
первое правило продолжало прятать все телефонные ссылки на сайте безусловно.

**Действие:** удалены оба CSS-правила (`a[href^="tel:"]{visibility:hidden}` и
`a.phone-fixed{visibility:visible!important}`) из custom CSS сайта, изменения
опубликованы для всего сайта (не только одной страницы — правка глобальная).

**Результат подтверждён** (Playwright, несколько страниц):
- LV главная: `tel:+37167660749` / `+371-67-660-749`, `visible: true`.
- RU главная: `tel:+37167660728` / `+37-167-660-728` (текст без пробела после
  кода страны — маска форматирует чуть иначе, чем LV, но цифры верны), `visible: true`.
- LV контакты (`/kontakti`): 2 из 3 номеров подменены (`+371-67-660-729`), третий
  остался статичным `+371-67-660-725` — вероятно, правило `contact lv` ещё не
  проверено на ГЕО-группу по аналогии с `header`.
- RU контакты (`/ru/kontakti`) — при проверке вернулась пустая страница/0
  найденных tel-ссылок, требует отдельной проверки URL/содержимого.

Хедер (LV+RU, обе копии) — полностью закрыт: виден, кликабелен, подменяется
корректно. Это была исходная задача сессии.

**Осталось на будущее** (не блокирует текущую задачу):
- Разобраться с третьим статичным номером на LV `/kontakti` (вероятно, тот же
  паттерн ГЕО-группы `not set`, что чинили для `header`).
- Проверить RU `/kontakti` отдельно.
- Поправить форматирование маски на RU-хедере (сейчас `+37-167-660-728`, ожидался
  бы формат `+371-67-660-728` как на LV) — визуальная мелочь, не критично для звонка.
- Пункты из предыдущей записи (footer ru, аудит остальных страниц) актуальны.

См. [[Задачи]].

## [2026-09-01] Ringostat | правило "contact lv" переименовано и исправлено | реально работало только на RU из-за бага в XPath | теперь покрывает LV и RU

Пользователь спросил, почему правило называется "contact lv", если должно
работать на обеих версиях. Проверка показала: несмотря на условие
`contains(.,'Adrese') or contains(.,'Адрес')` (предусматривающее оба языка),
правило реально давало 1 совпадение только на RU `/ru/kontakty`, на LV
`/kontakti` — 0.

Причина — последний шаг исходного XPath `/a[starts-with(@href,'tel:')]`
(одинарный слэш, "прямой потомок") не совпадал со структурой LV-страницы, где
номер лежит на 2 уровня глубже (`div.t-text` → `ul` → `li` → `a`), а не прямо
внутри `div.t-text`. На RU-версии, видимо, `<a>` лежит без обёртки списком —
поэтому там совпадало.

**Исправление:** заменён последний шаг на `//a[...]` (любой уровень
вложенности):

    //div[contains(@class,'t-container ')]//div[contains(@class,'t-text')][contains(.,'Adrese') or contains(.,'Адрес')]//a[starts-with(@href, 'tel:')]

Подтверждено (Playwright): теперь по 1 совпадению и на LV kontakti, и на RU
kontakty. Правило также переименовано (снят вводящий в заблуждение суффикс "lv").

## [2026-09-01] Ringostat | найдены два новых универсальных футер-правила вместо всех хрупких id-шных | охват полный, кликабельность сохранена, самодублирование исправлено

По итогам полной ревизии (см. записи выше) составлен и передан пользователю
план очистки: из 32 действующих правил оставить 3 универсальных (`header`,
`contact` (переименовано выше), `ringo-phone`), удалить 29 хрупких id-правил
(15 футерных + 14 хедерных дублей на RU-страницах), добавить 2 новых
универсальных футер-правила. План сохранён в скретчпад-файле сессии
(`ringostat_cleanup_plan.md`), не в вики — при повторном обращении к этой теме
пересобрать из этой записи и текущего состояния кабинета.

**Правило А "footer universal — link intact"** (для мест, где `<a href="tel:">`
ещё цела):

    XPath: //div[contains(@class,'t420__descr')][contains(.,'KONTAKTI') or contains(.,'КОНТАКТЫ')]//a[starts-with(@href,'tel:')][not(.//a)]
    Маска: <a href="tel:<t>">+###########</a>

**Правило Б "footer universal — text only"** (для мест, где `<a>` уже
испорчена предыдущими full-text масками, остался голый текст):

    XPath: //div[contains(@class,'t420__descr')][contains(.,'KONTAKTI') or contains(.,'КОНТАКТЫ')]//li[not(.//a)][contains(.,'660')]
    Маска: <a href="tel:<t>">+###########</a>

Пользователь внёс оба правила. Подтверждено (Playwright, 4 страницы — LV/RU
главная, LV/RU kontakti): кликабельность (`<a href="tel:">`) восстановлена
на ВСЕХ проверенных страницах, не только на тех, где раньше были отдельные
id-правила. Полный охват футера универсальным правилом подтверждён на практике.

**Найден и исправлен побочный эффект — самодублирование XPath** (тот же паттерн,
что был у ProfiMet): правило А без защиты от повторной обработки оборачивало
уже подменённый `<a>` ещё одним `<a>` при каждом новом прогоне скрипта.
Исправление — добавлен `[not(.//a)]` в конец XPath правила А (правило Б уже
было защищено этим условием по своей структуре). Проверено: старый XPath даёт
2 совпадения на уже обработанном элементе, исправленный — 1.

**Остаточный эффект, оставлен как есть по решению пользователя:** после
исправления вложенность `<a>` в HTML-маске остаётся на уровне, накопленном ДО
исправления (4 вложенных `<a>` на всех 4 проверенных страницах) — это записано
физически в саму HTML-маску блока в вёрстке Tilda за несколько прогонов до
фикса, дальше не растёт (подтверждено 3 последовательными свежими загрузками).
Не влияет на функциональность (звонок/клик работают), только на валидность
HTML-кода. Можно почистить вручную в Tilda позже, не критично.

**Ещё не сделано** (из первоначального плана очистки):
- Не удалены 29 старых хрупких id-правил (15 футерных + 14 хедерных дублей на
  RU) — они пока избыточны, но не мешают (новые универсальные правила их
  перекрывают). Безопасно удалить в любой момент.

## [2026-09-01] Ringostat | побочная находка при проверке ringo-phone | чужой номер на странице политики конфиденциальности RU | требует отдельного разбора, не связано с Ringostat

При проверке правила `ringo-phone` (должно матчить спец-класс `ringo-phone` в
длинных текстах юридических страниц) обнаружено: класс `ringo-phone` **не
найден нигде на сайте** — ни на `/politika-konfidentsialitates` (LV), ни на
`/ru/politika-konfidentsialnosti` (RU), хотя изначальные full-text маски (см.
самую первую запись сессии про большие маски) явно содержали
`<span class="ringo-phone"></span>` в этом тексте.

При разборе страницы `/ru/politika-konfidentsialnosti` найдено:
- Текст политики сейчас содержит **другой номер телефона**: `+371 20 368 830`
  — не совпадает ни с основным номером клиента, ни с каким-либо номером пула
  Ringostat. Природа этого номера неизвестна, пользователь не в курсе, откуда
  он взялся.
- В начале текста страницы присутствует строка `Error get alias` — похоже на
  техническую ошибку генерации/рендера контента, не относящуюся к теме
  подмены номеров.

Это НЕ связано с Ringostat или с работой этой сессии — текст страницы был
изменён кем-то независимо (пользователь не знает, кем и когда). Требует
отдельного разбора с клиентом: кто редактировал юридическую страницу и как
там оказался посторонний номер. См. срочную задачу в [[Задачи]].

См. [[Задачи]].

## [2026-09-01] Ringostat | старые хрупкие правила удалены пользователем | битая RU-ссылка хедера оказалась связана с реальной схемой переадресации звонков

Пользователь удалил все 29 старых хрупких id-правил из плана очистки —
кабинет Ringostat приведён к финальному виду (5 правил: `header`, `contact`
(переименовано), `ringo-phone`, два новых футер-правила).

**Уточнение по статичным номерам LV/RU:** проверено заново (сырой HTML,
до какой-либо подмены) — основной номер клиента одинаковый на LV и RU,
`+37167660725`. НЕ разные номера по умолчанию, вопреки первому впечатлению из
самой первой сессии.

**НО** — при этой проверке снова всплыла битая RU-ссылка хедера
(`tel:+37120007879` при видимом тексте `+371-67-660-725`), которую мы НЕ
трогали (правили только LV-версию этого пункта меню). Пользователь спросил,
не опасно ли это — если Ringostat меняет только видимый текст, а не href,
могла бы звонить не туда.

**Проверено эмпирически (Playwright, RU хедер с тестовым UTM):** Ringostat
переписывает `href` ПОЛНОСТЬЮ (`tel:+37167660728` вместо `tel:+37120007879`),
не только текст — то есть во время активной подмены эта битая ссылка
безопасна, клик уходит на подменный номер, а не на `37120007879`.

**Неожиданная находка при уточнении происхождения номера:** пользователь
проверил в кабинете Ringostat раздел "Схемы переадресации" (не связан с
подменой номеров на сайте — это настройка самой телефонии/распределения
звонков) — номер `37120007879` оказался РЕАЛЬНЫМ резервным номером в схеме
"Main": сотрудник Diana Edgars (101) принимает звонок, если не отвечает за
90 сек — переадресация на `37120007879`.

**Значит это может быть НЕ опечаткой**, а намеренно поставленным в RU-меню
номером (например, чтобы русскоязычные посетители звонили сразу на резервную
линию/конкретного сотрудника, минуя общую очередь) — правили же мы только LV
исходя из предположения, что там та же самая опечатка, что и в LV
(`tel:+337167660725` → неверный код страны). Для RU природа ссылки другая:
не "337" вместо "371" (искажение кода страны), а совершенно другой, при этом
РАБОЧИЙ и используемый в реальной схеме звонков номер.

**Решение: НЕ трогать RU-ссылку хедера**, пока не уточнено у клиента, было ли
это намеренно. Только для активного трафика с подходящим UTM это не имеет
значения (Ringostat подменяет полностью) — риск есть только для прямых
заходов/трафика без UTM на RU-версию сайта, где эта ссылка останется видна
как есть до выяснения.

Задача с уточнением зафиксирована в [[Задачи]] — не путать со срочной задачей
про чужой номер `+371 20 368 830` на странице политики конфиденциальности,
это два РАЗНЫХ номера и разные ситуации, оба требуют уточнения у клиента, но
не должны смешиваться при разборе.

См. [[Задачи]].
